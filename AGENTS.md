# 项目开发须知

## 真机调试优先级

- iPhone 或 iPad 已通过 Xcode 连接时，必须优先把已连接的实体设备直接设为 Xcode 的运行或测试目标，使用 Xcode 自带的构建、安装、运行、调试控制台、断点、XCTest 和设备管理能力。
- 已连接真机可用时，不要用模拟器代替真机验证。
- 真机 UI Test 不得使用会清空、替换或预置 App 沙盒数据的启动参数；这类破坏性测试只能在模拟器运行。真机自动化必须先证明测试路径不会修改用户真实数据。
- 不要把 iPhone 镜像作为常规真机调试手段。
- 只有在 Xcode 无法观察或操作系统级界面（例如系统文件保存器），或 Xcode 调试路径明确失败后，才使用 iPhone 镜像作为补充手段。
- 使用 iPhone 镜像前，先说明 Xcode 路径遇到的具体限制；验证完成后及时退出镜像。
